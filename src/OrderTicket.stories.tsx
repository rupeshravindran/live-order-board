import React from "react";
import OrderBoard from "./OrderBoard";

export default {
  title: "Live Orderboard World",
  component: OrderBoard,
};

export const Default = (): React.ReactNode => <OrderBoard />;
